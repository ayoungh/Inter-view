import { defineConfig, devices } from "@playwright/test";
export default defineConfig({ testDir: "./tests/e2e", use: { baseURL: "http://localhost:3003", trace: "retain-on-failure" }, projects: [{ name: "desktop", use: { ...devices["Desktop Chrome"] } }, { name: "narrow", use: { ...devices["Pixel 7"] } }], webServer: { command: "INTERVIEW_DEMO_MODE=1 INTERVIEWER_PASSWORD=playwright-secret SESSION_SECRET=playwright-session pnpm dev -p 3003", url: "http://localhost:3003/login", reuseExistingServer: !process.env.CI } });
